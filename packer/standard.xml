<domain type="kvm">
  <name>standard2</name>
  <memory unit="MiB">3072</memory>
  <vcpu placement="static">2</vcpu>

  <os firmware="efi">
    <type arch="x86_64" machine="q35">hvm</type>
    <loader readonly="yes" type="pflash">/usr/share/edk2/ovmf/OVMF_CODE.fd</loader>
    <nvram template="/usr/share/edk2/ovmf/OVMF_VARS.fd">/var/lib/libvirt/qemu/nvram/standard2_VARS.fd</nvram>
    <boot dev="hd"/>
  </os>

  <cpu mode="host-passthrough"/>

  <features>
    <acpi/>
    <apic/>
  </features>

  <clock offset="utc"/>

  <on_poweroff>destroy</on_poweroff>
  <on_reboot>restart</on_reboot>
  <on_crash>destroy</on_crash>

  <devices>

    <emulator>/usr/bin/qemu-system-x86_64</emulator>

    <!-- Main disk -->
    <disk type="volume" device="disk">
      <driver name="qemu" type="qcow2"/>
      <source pool="default" volume="standard-disk.qcow2"/>
      <target dev="vda" bus="virtio"/>
    </disk>

    <!-- Cloud-init ISO -->
    <disk type="file" device="cdrom">
      <driver name="qemu" type="raw"/>
      <source file="/var/lib/libvirt/images/standard-init.iso"/>
      <target dev="sda" bus="sata"/>
      <readonly/>
    </disk>

    <!-- Network -->
    <interface type="network">
      <source network="vm-network"/>
      <model type="virtio"/>
    </interface>

    <!-- Serial console -->
    <serial type="pty">
      <target port="0"/>
    </serial>

    <console type="pty">
      <target type="serial" port="0"/>
    </console>

    <!-- QEMU guest agent -->
    <channel type="unix">
      <target type="virtio" name="org.qemu.guest_agent.0"/>
    </channel>

    <!-- RNG -->
    <rng model="virtio">
      <backend model="random">/dev/urandom</backend>
    </rng>

    <!-- Balloon -->
    <memballoon model="virtio"/>

    <!-- Graphics -->
    <graphics type="vnc" autoport="yes" listen="127.0.0.1"/>

    <video>
      <model type="virtio"/>
    </video>

  </devices>
</domain>
