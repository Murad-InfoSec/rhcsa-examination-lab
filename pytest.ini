[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --html=tests/reports/report.html
    --self-contained-html
markers =
    integration: requires a live lab VM and SSH access (deselect with -m "not integration")
