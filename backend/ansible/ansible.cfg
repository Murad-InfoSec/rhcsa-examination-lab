[defaults]
host_key_checking = False
vault_password_file = ~/.ansible/vault_pass

[ssh_connection]
# tilde may not expand in all Ansible versions; ansible_checker.py also passes
# --vault-password-file explicitly for reliability.
ssh_args = -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null
